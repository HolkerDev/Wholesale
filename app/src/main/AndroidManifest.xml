<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.dev.holker.wholesale">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".StartServer"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity
                android:name=".activities.MainActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".activities.Login"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".activities.Order">
        </activity>
        <activity
                android:name=".activities.OrderDescription"
                android:parentActivityName=".activities.List">
        </activity>
        <activity
                android:name=".activities.Settings"
                android:parentActivityName=".activities.List">
        </activity>
        <activity android:name=".activities.List">
        </activity>
        <activity
                android:name=".activities.SignUp"
                android:parentActivityName=".activities.Login">
        </activity>
        <activity android:name=".activities.SignupDescription"
                  android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>